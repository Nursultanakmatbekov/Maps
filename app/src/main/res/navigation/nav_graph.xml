<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph.xml"
    app:startDestination="@id/mapDisplayFragment">
    <fragment
        android:id="@+id/mapDisplayFragment"
        android:name="com.nur.maps.ui.fragments.MapDisplayFragment"
        android:label="fragment_map_display"
        tools:layout="@layout/fragment_map_display" >
        <argument
            android:name="map"
            app:argType="string" />
    </fragment>
</navigation>